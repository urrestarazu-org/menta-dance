package com.menta.physical.domain.model;

/** A recurring course's difficulty level. Closed set, unlike {@link CourseId}'s open catalog data. */
public enum PhysicalCourseLevel {
    BEGINNER,
    INTERMEDIATE,
    ADVANCED
}
