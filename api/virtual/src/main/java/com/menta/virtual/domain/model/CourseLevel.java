package com.menta.virtual.domain.model;

/** Difficulty level of a {@link VirtualCourse} — a closed, small set, unlike {@link CourseCategory}. */
public enum CourseLevel {
    BEGINNER,
    INTERMEDIATE,
    ADVANCED
}
